{
  "name": "action-organize",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "PORT=8090 STATE_BACKEND=firestore GOOGLE_CLOUD_PROJECT=test-project PUBSUB_EMULATOR_HOST=localhost:8085 PUBSUB_TOPIC_NAME=mind-events PUBSUB_PUBLISH_ENABLED=false FIRESTORE_EMULATOR_HOST=localhost:8081 STORAGE_EMULATOR_HOST=http://localhost:4443 ORGANIZE_GCS_BUCKET=test-bucket LEASE_TTL_SECONDS=120 VERTEX_USE_REAL_API=false vitest run",
    "test:watch": "PORT=8090 STATE_BACKEND=firestore GOOGLE_CLOUD_PROJECT=test-project PUBSUB_EMULATOR_HOST=localhost:8085 PUBSUB_TOPIC_NAME=mind-events PUBSUB_PUBLISH_ENABLED=false FIRESTORE_EMULATOR_HOST=localhost:8081 STORAGE_EMULATOR_HOST=http://localhost:4443 ORGANIZE_GCS_BUCKET=test-bucket LEASE_TTL_SECONDS=120 VERTEX_USE_REAL_API=false vitest"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.3",
    "@google-cloud/pubsub": "^5.2.0",
    "@google-cloud/storage": "^7.16.0",
    "express": "^5.1.0",
    "pino": "^9.7.0",
    "pino-http": "^10.5.0",
    "pino-pretty": "^13.1.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0"
  }
}
